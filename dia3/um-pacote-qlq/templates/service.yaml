apiVersion: v1
kind: Service
metadata:
  name: {{ include "um-pacote-qlq.fullname" . }}
  labels:
    {{- include "um-pacote-qlq.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "um-pacote-qlq.selectorLabels" . | nindent 4 }}
